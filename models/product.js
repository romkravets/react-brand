class Product {
   constructor(id, ownerId, title, imageUrl, descriprion, price) {
      this.id = id;
      this.ownerId = ownerId;
      this.title = title;
      this.imageUrl = imageUrl;
      this.descriprion = descriprion;
      this.price = price;
   }
}